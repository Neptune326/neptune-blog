# ===================================================
# 测试环境配置（test）
# 与开发环境保持一致，可按需修改数据库地址
# ===================================================
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root

# 日志：测试环境同样输出 debug 级别
logging:
  level:
    com.blog: debug
    org.springframework.web: debug
