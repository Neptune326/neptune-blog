# ===================================================
# 开发环境配置（dev）
# ===================================================
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root

# 日志：开发环境输出 debug 级别，方便排查问题
logging:
  level:
    com.blog: debug
    org.springframework.web: debug
