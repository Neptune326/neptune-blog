<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 标签 Mapper XML -->
<mapper namespace="com.blog.mapper.TagMapper">

    <!-- 查询所有标签及其关联的文章数量 -->
    <select id="selectTagWithArticleCount" resultType="com.blog.vo.TagVO">
        SELECT t.id,
               t.name,
               COUNT(at.article_id) AS articleCount
        FROM tag t
                 LEFT JOIN article_tag at ON at.tag_id = t.id
                 LEFT JOIN article a ON a.id = at.article_id AND a.deleted = 0
        GROUP BY t.id, t.name
        ORDER BY t.id ASC
    </select>

</mapper>
